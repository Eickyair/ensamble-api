services:
  - type: web
    name: random-forest-api
    env: python
    buildCommand: "pip install -r app/requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port 10000 --timeout-keep-alive 30"